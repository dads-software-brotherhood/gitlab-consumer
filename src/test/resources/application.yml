spring:
  main:
    banner-mode: "off"
  output:
    ansi:
      enabled: ALWAYS
  logging:
    level:
        root: INFO
        mx.dads.infotec.core: DEBUG

application:
    gitlab:
        api-url: http://localhost
        security:
            token: dummy
            token-header-name: private_token
